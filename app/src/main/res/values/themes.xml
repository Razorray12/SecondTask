<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme" parent="Theme.Material3.DayNight.NoActionBar" />
    <!-- Customize your light theme here. -->
    <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    <style name="Theme.Custom" parent="Base.Theme">
        <item name="android:windowBackground">@color/custom</item>
        <item name="android:colorBackground">@color/custom</item>
    </style>
</resources>